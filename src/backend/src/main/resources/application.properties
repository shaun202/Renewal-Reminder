# Server port
# Created by Claude on 12/8/2026
server.port=8080
spring.application.name=renewal-reminder

# Where the JSON data files live (relative to the working directory the app is started from)
app.data.dir=./data

# Pretty-print JSON files so they stay human-readable/diffable
app.data.pretty-print=true
