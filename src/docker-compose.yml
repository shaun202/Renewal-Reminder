services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    volumes:
      - ./backend/data:/app/data   # keeps items.json/categories.json on your machine between restarts

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
